#include <stdio.h>

/*
Triangle Shape 
By:AYS
*/

void main()
{
   int i,j,r;
   printf("Input number of rows (Triangle) :");
   scanf("%d",&r);
   for(i=0;i<=r;i++)
   {
     for(j=1;j<=r-i;j++)
     printf(" ");
     for(j=1;j<=2*i-1;j++)
       printf("*");
     printf("\n");
   }
}
